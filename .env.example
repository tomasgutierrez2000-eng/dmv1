# Google Gemini API key (for data-model agent). Get one at https://aistudio.google.com/apikey
GOOGLE_GEMINI_API_KEY=

# Optional: password-protect the agent chat to prevent unauthorized token usage
AGENT_PASSWORD=
