# Llama via Ollama (open-source, local). No API key. Install: https://ollama.com
# Then run: ollama pull llama3.2  (or another model that supports tool calling)
# Set AGENT_PROVIDER=llama to force Ask the Model to use only Llama (ignore Claude/Gemini).
AGENT_PROVIDER=llama
OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# Google Gemini API key (fallback). Get one at https://aistudio.google.com/apikey
GOOGLE_GEMINI_API_KEY=

# Optional: password-protect the agent chat to prevent unauthorized token usage
AGENT_PASSWORD=
